import { spacing } from './spacing';

/** Shared page gutters/insets so all screens align consistently. */
export const screenLayout = {
  horizontal: spacing.xl,
  top: spacing.sm,
  bottom: spacing.xxl,
} as const;

