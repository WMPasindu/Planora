# Planora Frontend (Web)

This folder is reserved for the web frontend application.

Suggested next structure:

- `frontend/app` (web app source)
- `frontend/package.json`
- `frontend/tsconfig.json`
- `frontend/.env.example`

Currently scaffolded as an empty placeholder by request.

